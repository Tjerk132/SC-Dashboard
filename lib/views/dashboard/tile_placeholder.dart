import 'package:flutter/material.dart';

class TilePlaceholder extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Placeholder Tile'),
    );
  }
}
